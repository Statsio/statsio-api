$kind: http-request
url: "{{base_url}}/api/channels?per_page=15"
method: GET
headers:
  Accept-Language: "{{language}}"
  Authorization: "{{token_type}} {{token}}"
queryParams:
  per_page: "15"
order: 1000
