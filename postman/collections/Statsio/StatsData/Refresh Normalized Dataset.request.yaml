$kind: http-request
url: "{{base_url}}/api/statsdata/{{statsdata_document_id}}/sources/{{statsdata_source_id}}/refresh"
method: POST
headers:
  Accept-Language: "{{language}}"
  Authorization: "{{token_type}} {{token}}"
order: 3075
