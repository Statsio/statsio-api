$kind: http-request
url: "{{base_url}}/api/statsdata/{{statsdata_document_id}}"
method: PATCH
headers:
  Accept-Language: "{{language}}"
  Authorization: "{{token_type}} {{token}}"
  Content-Type: application/json
body:
  type: json
  content: |
    {
      "title": "Mon rapport (édité)",
      "subtitle": "Sous-titre",
      "visibility": "public",
      "blocks": [],
      "dataSources": []
    }
order: 3010
