$kind: http-request
url: "{{base_url}}/api/statsdata/{{statsdata_document_id}}/sources/probe"
method: POST
headers:
  Accept-Language: "{{language}}"
  Authorization: "{{token_type}} {{token}}"
  Content-Type: application/json
body:
  type: json
  content: |
    {
      "url": "https://api.example.com/data",
      "apiKey": ""
    }
order: 3040
